spring:
  jpa:
    hibernate:
      ddl-auto: none   # schema.sql owns DDL
      naming:
        # Usa os nomes de @Table/@Column literalmente (sem converter camelCase->snake_case),
        # para casar com o schema.sql existente — ex.: a tabela "itensEstoque" (camelCase).
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
  datasource:
    url: jdbc:h2:mem:pizzadb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  sql:
    init:
      mode: always   # executa schema.sql no boot
    schema-locations: classpath:schema.sql
    data-locations: classpath:data.sql
  h2:
    console:
      enabled: true
      path: /h2
logging:
  level:
    org:
      springframework:
        jdbc: DEBUG
    com:
      bcopstein: DEBUG
application:
  name: ex4-lancheriaddd-v1
server:
  port: 8080
imposto:
  lei-vigente: "0412/2022"